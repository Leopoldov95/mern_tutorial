FULL CIRCLE STACK REQUEST
- first, declare the router 

// pushing project online
- do not immedtaley uplad to gh
- recall we need dotenv to get .env to work
- okay to ignore .env for public github HOWEVER we need .env for heroku api deployment
- heroku only needs .env file CONNECTION_URL
- heroku needs .env file
- so we ONLY push the db to heroku (rewatch video if I need to change the deploy server settiings)